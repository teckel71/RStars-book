# Archivos de sesión local de R
.RData
.Rhistory
.Rproj.user/

# Archivos generados por bookdown al compilar capítulos sueltos
# (caché temporal en la raíz del proyecto, NO la carpeta docs/ publicada)
/_book/
/_main_files/
/_main.log

# RStudio Connect / shinyapps.io
rsconnect/

# IMPORTANTE: docs/ y docs/*_files/figure-html/ NUNCA deben ignorarse,
# son el contenido publicado del libro (GitHub Pages lo sirve desde ahí).
